import { makeStyles } from "@material-ui/core/styles";

const useStyles = makeStyles(() => ({
  container: {
    marginTop: "20px",
  },
  icon: {
    marginRight: "20px",
  },
  button: {
    marginTop: "20px",
  },
}));

export { useStyles };
